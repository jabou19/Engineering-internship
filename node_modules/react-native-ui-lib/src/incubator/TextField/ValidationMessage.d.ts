import React from 'react';
import { ValidationMessageProps } from './types';
declare const ValidationMessage: {
    ({ validationMessage, enableErrors, validationMessageStyle, retainSpace, validate, testID }: ValidationMessageProps): React.JSX.Element | null;
    displayName: string;
};
export default ValidationMessage;
